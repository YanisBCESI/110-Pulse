{% extends "base.html.twig" %}
{% block body %}

<header class="entete">
    <div class="dropdown">
        <span class="dropspan" data-action="click->dropdown#toggle">Fichier</span>
        <div class="dropdown-content" data-dropdown-target="menu">
            <!--Mettre ici des <span> ou des <a> pour chaque option-->
            <span>TEST</span>
            <span>TEST</span>
            <span>TEST</span>
            <span>TEST</span>
        </div>
    </div>
</header>

{% endblock %}